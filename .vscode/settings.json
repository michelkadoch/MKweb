{
  "// note": "Comfortable defaults for editing prose. Adjust the font sizes to taste.",
  "editor.fontSize": 18,
  "editor.wordWrap": "on",
  "editor.lineNumbers": "off",
  "editor.minimap.enabled": false,
  "workbench.startupEditor": "readme",
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000,

  "// python": "Use the .venv created by the setup script so Preview can run plots.",
  "// python-note": "This path is for Windows. On Linux/macOS change it to ${workspaceFolder}/.venv/bin/python (or pick it once via 'Python: Select Interpreter').",
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/Scripts/python.exe",
  "python.terminal.activateEnvironment": true
}
